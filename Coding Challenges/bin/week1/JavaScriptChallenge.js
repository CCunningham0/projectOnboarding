/**
 * 
 */

function extractIng() {
	let str = "Jumping across the rails of the ring";
	
	let newStr = str.split(" ");
	newStr.forEach(word => {
		if (word.length > 4) {
			
		}
	});

	// console.log(newStr);
}

extractIng();